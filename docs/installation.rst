============
Installation
============

At the command line::

    $ easy_install pystackalytics

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pystackalytics
    $ pip install pystackalytics
